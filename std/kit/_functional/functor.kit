trait Functor: T[A] {
    function fmap[B](f: (A) -> B, v: T[A]): T[B];
}

implement Functor for Option {
    function fmap[B](f: (A) -> B, v: Option[A]): Option[B] {
        match v {
            Some(x): Some(f(x));
            None: None;
        }
    }
}
